import 'Notification.dart';

class Push extends Notification {

  Push(DateTime date, String message) : super(date,message);

  @override
  void sendNotification() {
    
  }
  
  @override
  void update(String) {
   
  }
 
} 