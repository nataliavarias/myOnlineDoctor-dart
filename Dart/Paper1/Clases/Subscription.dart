class Subscription {

  // Atributos
    DateTime expirationDate;

  // Constructor
     Subscription(this.expirationDate);

  // Métodos
  void changeStatus(Status) {
    
  }

  void changeRenewalP(RenewalPeriod) {
    
  }

  void processPayment(PaymentMethod) {
    
  }
 

} 